<!DOCTYPE html>
<html>
  <head>
    <h1 style= "font-family:Arial; font-size: 40px;"> <%= current_user.full_name%>'s User Profile </h1>
    <title> Your User Profile</title>
  </head>
  <br>
  <br>
  <br>
  <br>


<table border="1" cellpadding="10" style="font-size:30px">
  <tr align="left" style="background-color:#61c84c; color:white">
    <th>Name</th>
    <th>Email</th>
</tr>

  <tr style="background-color:#eeedc8">
    <td><%= current_user.full_name %></td>
    <td><%= current_user.email %></td>
    </tr>
</table>

    <br>
    <br>
    <br>
    <br>
    <div>
      <h5><%= link_to "Edit your account", edit_user_path(current_user.id) %></h5>
    </div>

    <div>
      <h5><%= link_to "View past orders", user_profile_path %></h5>
    </div>

    <div style= "left; font-family:Arial; font-size:30px;">
      <h2>My Store</h2>
    <% if current_user.store_id != nil %>
        <br>
      <div style= "left; font-family:Arial; font-size:25px;"
        <br>
        <% store_path = Store.find(current_user.store_id).path%>
        <%= link_to "View My Store", store_home_path(store_path) %>
        <%= link_to "Edit My Store",
          edit_store_path(current_user.store_id) %>
      </div>
      <br>
    <% else %>
      <div style= "left; font-family:Arial; font-size:20px; ">
       <%= link_to "You don't have a store. Click to create a store", new_store_path %>
    <% end %>
    </div>

  </body>
</html>
