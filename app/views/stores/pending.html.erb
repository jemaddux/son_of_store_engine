<p>Your store (<%= @store.name %>) has been created at (example.com/<%= @store.path %>) with the description "<%= @store.description %>" and is pending administrator approval before it goes live.</p>
